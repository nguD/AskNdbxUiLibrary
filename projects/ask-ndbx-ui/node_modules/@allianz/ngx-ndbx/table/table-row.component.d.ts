import { ChangeDetectorRef, ElementRef } from '@angular/core';
import { EventEmitter } from '@angular/core';
/**
 * This is a table row.
 */
export declare class NxTableRowComponent {
    protected _changeDetectorRef: ChangeDetectorRef;
    private _elementRef;
    /** Whether this table row is selectable */
    selectable: boolean;
    private _selectable;
    /** Whether this table row is selected */
    selected: boolean;
    private _selected;
    select: EventEmitter<void>;
    constructor(_changeDetectorRef: ChangeDetectorRef, _elementRef: ElementRef);
    _onSelect($event: any): void;
    /**
     * Checks if the event would trigger an action.
     * Return `true` if a button, link, input or label are clicked.
     */
    private isActionEvent;
}
