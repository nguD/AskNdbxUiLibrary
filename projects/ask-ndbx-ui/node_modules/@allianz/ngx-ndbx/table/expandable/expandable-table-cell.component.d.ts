import { ChangeDetectorRef, OnDestroy } from '@angular/core';
import { NxExpandableTableRowComponent } from './expandable-table-row.component';
/**
 * This is an expandable table cell.
 * Only works in conjunction with the `[nxExpandableTableRow]` component.
 */
export declare class NxExpandableTableCellComponent implements OnDestroy {
    private _changeDetectorRef;
    private _destroyed;
    _open: boolean;
    constructor(_row: NxExpandableTableRowComponent, _changeDetectorRef: ChangeDetectorRef);
    ngOnDestroy(): void;
}
