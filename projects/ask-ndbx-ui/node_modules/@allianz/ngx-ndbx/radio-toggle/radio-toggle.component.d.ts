import { AfterContentInit, AfterViewInit, OnDestroy, QueryList, Renderer2, ChangeDetectorRef, DoCheck } from '@angular/core';
import { ControlValueAccessor, FormGroupDirective, NgControl, NgForm } from '@angular/forms';
import { Subject } from 'rxjs';
import { ErrorStateMatcher } from '@allianz/ngx-ndbx/utils';
import { NxRadioToggleButtonBaseComponent } from './radio-toggle-button-base.component';
export declare class NxRadioToggleComponent implements ControlValueAccessor, AfterViewInit, OnDestroy, AfterContentInit, DoCheck {
    private renderer;
    private _changeDetectorRef;
    /** @docs-private */
    ngControl: NgControl;
    private _errorStateMatcher;
    private _parentForm;
    private _parentFormGroup;
    private _toggleId;
    private _selection;
    private _destroyed;
    private _disabled;
    /** @docs-private */
    errorState: boolean;
    _disableChange: Subject<void>;
    /** Sets the component to the disabled state.*/
    disabled: boolean;
    private toggleContainer;
    private _name;
    /** Sets the name used for accessibility. */
    name: string;
    /** @docs-private */
    toggleButtons: QueryList<NxRadioToggleButtonBaseComponent>;
    private onTouchedCallback;
    private onChangeCallback;
    constructor(renderer: Renderer2, _changeDetectorRef: ChangeDetectorRef, 
    /** @docs-private */
    ngControl: NgControl, _errorStateMatcher: ErrorStateMatcher, _parentForm: NgForm, _parentFormGroup: FormGroupDirective);
    ngAfterViewInit(): void;
    ngAfterContentInit(): void;
    ngOnDestroy(): void;
    ngDoCheck(): void;
    /** @docs-private */
    subscribeToToggleButtonsChange(): void;
    registerOnChange(onChangeCallback: any): void;
    registerOnTouched(onTouchedCallback: any): void;
    /** Preselects the respective options. */
    writeValue(value: string): void;
    setDisabledState(isDisabled: boolean): void;
    /** @docs-private */
    readonly id: string;
    /** Sets the modifiers for the component. */
    style: string;
    /** @docs-private */
    selection: any;
    /** @docs-private */
    change(value: any): void;
    /** @docs-private */
    updateErrorState(): void;
}
