import { OnDestroy, OnInit, TemplateRef, ViewContainerRef } from '@angular/core';
import { NxModalService } from './modal.service';
export declare class NxOpenModalOnClickDirective implements OnInit, OnDestroy {
    private templateRef;
    private viewContainer;
    private modalService;
    /** @docs-private */
    elements: Array<any>;
    private subscription;
    constructor(templateRef: TemplateRef<any>, viewContainer: ViewContainerRef, modalService: NxModalService);
    ngOnInit(): void;
    ngOnDestroy(): void;
    /** @docs-private */
    clickHandler: (event: any) => void;
    /**
     * One or multiple template reference variables pointing to elements
     * which should trigger opening the modal on click.
     *
     * Value: A single template reference variable or an array of template reference variables.
     */
    nxOpenModalOnClick: any;
}
