import { DataSource } from '@angular/cdk/collections';
import { BehaviorSubject, Observable } from 'rxjs';
export declare class TableDataSource extends DataSource<any> {
    private _datachange;
    constructor(_datachange: BehaviorSubject<any[]>);
    /** @docs-private Connect function called by the table to retrieve one stream containing the data to render. */
    connect(): Observable<Object[]>;
    /** @docs-private */
    disconnect(): void;
}
