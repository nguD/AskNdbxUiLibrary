export declare class NxProgressbarComponent {
    /** @docs-private */
    progressbarId: string;
    /** Sets the value of the progress bar. Defaults to zero. Mirrored to aria-value now. */
    value: number;
    private _value;
    _primaryTransform(): {
        transform: string;
    };
}
