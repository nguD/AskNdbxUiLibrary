import { ElementRef, OnInit } from '@angular/core';
export declare type RowJustification = 'start' | 'end' | 'center' | 'between' | 'around';
export declare type RowContentAlignment = 'start' | 'end' | 'center' | 'between' | 'around' | 'stretch';
export declare type RowItemsAlignment = 'start' | 'end' | 'center' | 'between' | 'stretch';
export declare type RowWrapping = 'wrap' | 'nowrap' | 'reverse';
export declare class NxRowComponent implements OnInit {
    private el;
    private ROW;
    private ROW_RESERVE;
    _classNames: string;
    /** General */
    private _nxRow;
    /**
     * Values: row | row-reverse
     *
     * Default value: row
     */
    nxRow: string;
    /** Align items on the main axis (horizontally). */
    nxRowJustify: RowJustification;
    /** Similar to nxRowAlignItems, but instead of aligning flex items, it aligns flex lines. */
    nxRowAlignContent: RowContentAlignment;
    /** The default alignment for items inside the flexible container. */
    nxRowAlignItems: RowItemsAlignment;
    /** How the flexible items should be wrapped. */
    nxRowWrap: RowWrapping;
    constructor(el: ElementRef);
    ngOnInit(): void;
}
