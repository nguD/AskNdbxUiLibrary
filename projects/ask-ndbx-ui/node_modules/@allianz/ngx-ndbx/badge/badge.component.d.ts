import { ChangeDetectorRef } from '@angular/core';
/** Possible badge types. */
export declare type NxBadgeType = 'active' | 'positive' | 'critical' | 'negative';
export declare class NxBadgeComponent {
    private _changeDetRef;
    private _type;
    private _vibrant;
    constructor(_changeDetRef: ChangeDetectorRef);
    /** Sets the class name for the badge element.  */
    type: NxBadgeType;
    /** Change badge style to vibrant. */
    vibrant: boolean;
}
