import { ElementRef } from '@angular/core';
/** Types of icon sizes */
export declare type IconSize = 'auto' | 's' | 'm' | 'l' | 'xl';
export declare class NxIconComponent {
    /**@docs-private */
    el: ElementRef;
    private _name;
    /** Sets the name for specifing the icon.*/
    name: string;
    /** Whether the icon has an outline. */
    outline: boolean;
    /** Whether the icon is filled. */
    fill: boolean;
    private _size;
    /** Specifies the size of the icon. */
    size: IconSize;
    constructor(
    /**@docs-private */
    el: ElementRef);
}
