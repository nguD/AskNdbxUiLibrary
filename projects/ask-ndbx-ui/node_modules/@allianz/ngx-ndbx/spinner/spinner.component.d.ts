import { ChangeDetectorRef } from '@angular/core';
/** Options for sizing of the spinner. */
export declare type SpinnerSize = 'small' | 'medium' | 'large';
export declare class NxSpinnerComponent {
    private _changeDetectorRef;
    private _size;
    /** Sets the size of the spinner. Default is 'small'. */
    size: SpinnerSize;
    private _negative;
    /** Whether the spinner should use a negative styling. */
    negative: boolean;
    constructor(_changeDetectorRef: ChangeDetectorRef);
}
