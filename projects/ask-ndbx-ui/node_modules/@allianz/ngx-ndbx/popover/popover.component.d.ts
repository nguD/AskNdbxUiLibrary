import { EventEmitter, OnDestroy, TemplateRef } from '@angular/core';
import { Subject } from 'rxjs';
import { NxPopoverContentDirective } from './popover-content';
export declare class NxPopoverComponent implements OnDestroy {
    /** @docs-private */
    templateRef: TemplateRef<any>;
    /** Content that will be rendered lazily. */
    _lazyContent: NxPopoverContentDirective;
    /** Event emitted when the popover is closed. */
    closed: EventEmitter<void>;
    /** @docs-private */
    closeButtonClick: Subject<void>;
    /** @docs-private */
    id: string;
    /** @docs-private */
    direction: any;
    /** @docs-private */
    showCloseButton: boolean;
    /** @docs-private */
    arrowStyle: {};
    constructor();
    ngOnDestroy(): void;
    /** @docs-private */
    emitCloseButtonClick(): void;
    /** @docs-private */
    _onCloseKeyup($event: any): void;
    /** @docs-private */
    emitClosedEvent(): void;
    /** @docs-private */
    readonly classList: string[];
    /** Prevent the popover from closing when the user clicks on the popover content. */
    _onClick(event: any): void;
}
