import * as ts from 'typescript';
export interface FileImportMetadata {
    hasCircleToggleImport: boolean;
    namedImportsWithOldModule: ts.NamedImports[];
}
export declare class ModuleMetadata extends Map<ts.PropertyAssignment, ts.Identifier[]> {
}
/**
 * Visitor which scans the source file for imports from NxCheckboxCircleModule, NxRadioCircleModule
 * and NxCircleModule as well as for any property assignments in objects which fit the ngModule and
 * configureTestingModule structure.
 */
export declare class DocumentVisitor {
    typeChecker: ts.TypeChecker;
    importsMap: Map<ts.SourceFile, FileImportMetadata>;
    propertyMap: Map<ts.SourceFile, ts.PropertyAssignment[]>;
    constructor(typeChecker: ts.TypeChecker);
    visitNode(node: ts.Node): void;
    visitNamedImport(node: ts.NamedImports): void;
    visitPropertyAssignment(node: ts.PropertyAssignment): void;
}
