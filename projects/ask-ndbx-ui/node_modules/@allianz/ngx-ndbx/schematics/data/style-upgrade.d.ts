export interface StyleSelectorUpgradeData {
    /** The style selector to replace. */
    replace: string;
    /** The new name for the selector. */
    replaceWith: string;
}
export declare const selectorChange: StyleSelectorUpgradeData[];
