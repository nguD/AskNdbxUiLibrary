import { AttributeUpgradeData } from '../data/attribute-upgrade';
/**
 * gets a source file and returns the migrated string
 * @param attributeChange The attributes that should be migrated
 * @param htmlSource
 * @param fileName
 */
export declare function migrateHtml(attributeChange: AttributeUpgradeData[], htmlSource: string, fileName: string): string | null;
