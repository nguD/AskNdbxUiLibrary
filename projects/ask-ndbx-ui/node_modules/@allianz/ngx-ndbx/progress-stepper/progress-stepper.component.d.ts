import { CdkStep, CdkStepper } from '@angular/cdk/stepper';
import { AfterContentInit, QueryList, OnDestroy, ElementRef, ChangeDetectorRef } from '@angular/core';
import { ErrorStateMatcher } from '@allianz/ngx-ndbx/utils';
import { FormControl, FormGroupDirective, NgForm } from '@angular/forms';
import { Subject } from 'rxjs';
import { Directionality } from '@angular/cdk/bidi';
export declare class NxStepComponent extends CdkStep implements ErrorStateMatcher, OnDestroy {
    private stepper;
    private _errorStateMatcher;
    _destroyed: Subject<boolean>;
    constructor(stepper: NxProgressStepperDirective, _errorStateMatcher: ErrorStateMatcher);
    /** The top level abstract control of the step. */
    stepControl: any;
    private _stepControl;
    /** Whether the user has seen the expanded step content or not. */
    interacted: boolean;
    private _interacted;
    /** Custom error state matcher that checks for validity of the step form. */
    isErrorState(control: FormControl | null, form: FormGroupDirective | NgForm | null): boolean;
    ngOnDestroy(): void;
}
/** @docs-private */
export declare class NxProgressStepperDirective extends CdkStepper implements AfterContentInit {
    private _cdRef;
    _steps: QueryList<NxStepComponent>;
    _stepHeader: QueryList<null>;
    /** Sets the label on the left side showing the current step label. Used for mobile viewports. */
    currentStepLabel: string;
    /** Sets the title to be rendered above the progress bar.
     * @deletion-target 10.0.0
     * @deprecated Use `nx-label` instead */
    title: string;
    constructor(_cdRef: ChangeDetectorRef, _dir: Directionality, _elementRef: ElementRef<HTMLElement>, _document?: any);
    ngAfterContentInit(): void;
    /** @docs-private */
    readonly hasPrevious: boolean;
    /** @docs-private */
    readonly hasNext: boolean;
    /** @docs-private */
    readonly count: number;
    /** @docs-private */
    readonly currentStep: NxStepComponent | null;
    /** @docs-private */
    readonly nextStep: NxStepComponent | null;
    /** @docs-private */
    readonly steps: QueryList<NxStepComponent>;
    /** @docs-private */
    readonly currentLabel: string;
}
