import { QueryList, ChangeDetectorRef } from '@angular/core';
import { NxStepComponent } from '../progress-stepper.component';
export declare class NxMultiStepperGroupComponent {
    private _changeDetectorRef;
    /** @docs-private */
    steps: QueryList<NxStepComponent>;
    /** Sets the label of a group that is shown to the user. */
    label: string;
    private _label;
    constructor(_changeDetectorRef: ChangeDetectorRef);
}
