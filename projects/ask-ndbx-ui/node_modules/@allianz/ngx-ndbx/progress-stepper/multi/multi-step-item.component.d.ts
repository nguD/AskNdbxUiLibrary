import { ChangeDetectorRef } from '@angular/core';
import { NxMultiStepperDirection } from './multi-step.component';
/** @docs-private */
export declare class NxMultiStepItemComponent {
    private _changeDetectorRef;
    /** The direction of the step */
    direction: NxMultiStepperDirection;
    private _direction;
    /** The label of the step. */
    label: string;
    /** Sets the selected step. */
    selected: boolean;
    private _selected;
    /** Sets the active step. */
    active: boolean;
    private _active;
    disabled: boolean;
    private _disabled;
    /** Sets the last step. */
    last: boolean;
    private _last;
    /** Sets the step completed. */
    completed: boolean;
    private _completed;
    constructor(_changeDetectorRef: ChangeDetectorRef);
}
